# Air Hands Number Drawing

A Computer Vision application that lets you draw digits in the air using a colored marker/object via webcam and predicts the drawn digit ($0\text{--}9$) in real time using a Convolutional Neural Network (CNN) trained on the MNIST dataset.
